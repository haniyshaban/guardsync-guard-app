# GuardWise Companion App Environment Variables

# API Configuration - Points to guardwise-platform Express server
# Using the proxy path since Vite will forward /api requests to the backend
VITE_API_URL=/api

# Environment
VITE_ENV=development
